import { OverviewCutterPipe } from './overview-cutter.pipe';

describe('OverviewCutterPipe', () => {
  it('create an instance', () => {
    const pipe = new OverviewCutterPipe();
    expect(pipe).toBeTruthy();
  });
});
